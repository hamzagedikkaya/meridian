<% content_for :page_title, "Edit todo" %>
<% content_for :nav_key, "todos" %>

<div class="max-w-2xl space-y-4">
  <%= link_to "← Back", todos_path, class: "text-sm text-[var(--color-fg-muted)]" %>
  <h2 class="font-[family-name:var(--font-display)] text-3xl tracking-tight">Edit todo</h2>
  <div class="card"><%= render "form", todo: @todo %></div>
</div>
