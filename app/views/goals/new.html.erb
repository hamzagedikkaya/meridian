<% content_for :page_title, "New goal" %>
<% content_for :nav_key, "goals" %>

<div class="max-w-2xl space-y-4">
  <%= link_to "← Back", goals_path, class: "text-sm text-[var(--color-fg-muted)]" %>
  <h2 class="font-[family-name:var(--font-display)] text-3xl tracking-tight">New goal</h2>
  <div class="card"><%= render "form", goal: @goal %></div>
</div>
