<%# Shared chrome for all Settings pages — header + horizontal tab pill + content. %>
<div class="max-w-4xl space-y-6">
  <div>
    <h1 class="font-[family-name:var(--font-display)] text-4xl tracking-tight"><%= t("settings.title") %></h1>
    <p class="mt-1 text-[var(--color-fg-muted)]"><%= t("settings.subtitle") %></p>
  </div>

  <%= render "tabs" %>

  <%= yield %>
</div>
