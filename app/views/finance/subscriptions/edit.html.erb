<% content_for :page_title, "Edit subscription" %>
<% content_for :nav_key, "finance" %>

<div class="max-w-2xl space-y-4">
  <%= link_to "← Back", finance_subscriptions_path, class: "text-sm text-[var(--color-fg-muted)]" %>
  <h2 class="font-[family-name:var(--font-display)] text-3xl tracking-tight">Edit subscription</h2>
  <div class="card"><%= render "form", subscription: @subscription %></div>
</div>
